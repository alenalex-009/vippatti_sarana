# Placeholder env file — the real keys live in app/.env (git-ignored).
# The build reads GNEWS_API_KEY / FIRMS_MAP_KEY from app/.env when present and
# falls back to this file so clean clones still build; the app then honestly
# reports each source as "not configured" until a real key is added.
GNEWS_API_KEY=YOUR_GN…HERE
FIRMS_MAP_KEY=YOUR_FI…HERE
